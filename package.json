{
  "name": "habit-garden",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "deploy": "pnpm --filter web build && firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^15.29.0"
  },
  "pnpm": {
    "overrides": {
      "form-data@<2.5.6": "2.5.6",
      "websocket-driver@<0.7.5": "0.7.5",
      "protobufjs@<=7.6.4": "7.6.5"
    }
  }
}
